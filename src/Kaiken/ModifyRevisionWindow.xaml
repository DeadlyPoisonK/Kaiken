<Window x:Class="Kaiken.ModifyRevisionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Modificar Código de Revisión" Height="320" Width="440"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <StackPanel Margin="16">
        <TextBlock Text="Nuevo Código de Revisión:" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBox x:Name="RevisionTextBox" Text="RD" Margin="0,0,0,12" FontSize="14" Padding="4"/>

        <TextBlock Text="Aplicar a:" FontWeight="Bold" Margin="0,0,0,4"/>
        <ComboBox x:Name="SetComboBox" Margin="0,0,0,12"
                  ToolTip="Elige un Sheet Set (Publish Set) para modificar solo esas láminas, sin tocar la revisión de las demás."/>

        <CheckBox x:Name="FilterLength29CheckBox" Content="Procesar solo láminas con Número de Plano &gt; 29 caracteres"
                  IsChecked="True" Margin="0,0,0,8"
                  ToolTip="Misma regla del script de Dynamo para omitir portadas u otras láminas secundarias."/>

        <CheckBox x:Name="UpdateParamCheckBox" Content="Actualizar parámetro 'INFO_10_Revision'"
                  IsChecked="True" Margin="0,0,0,8"
                  ToolTip="Escribe el código de revisión en el parámetro de lámina INFO_10_Revision."/>

        <CheckBox x:Name="UpdateSheetNumberCheckBox" Content="Actualizar 10º segmento del Número de Plano (separador '-')"
                  IsChecked="True" Margin="0,0,0,16"
                  ToolTip="Reemplaza el índice 9 (10º segmento) del número de plano con la nueva revisión."/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar" Width="80" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Aplicar" Width="90" IsDefault="True" Click="Apply_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
