<Window x:Class="Kaiken.ManageSetRevisionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar Revisiones del Set" Height="460" Width="460"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Destildá las revisiones que querés sacar de todas las láminas del set:"
                   FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" x:Name="SetNameText" Foreground="Gray" Margin="0,0,0,8"/>

        <Border Grid.Row="2" BorderBrush="#CCCCCC" BorderThickness="1" Margin="0,0,0,8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="RevisionsList" Margin="4">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Label}" IsChecked="{Binding IsChecked}" Margin="4,4,4,4"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <TextBlock Grid.Row="3" x:Name="NoteText" FontStyle="Italic" Foreground="Gray"
                   TextWrapping="Wrap" Margin="0,0,0,8" Visibility="Collapsed"/>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Grid.Column="0">
                <Button Content="Tildar todas" Width="100" Margin="0,0,8,0" Click="CheckAll_Click"/>
                <Button Content="Destildar todas" Width="110" Click="UncheckAll_Click"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Column="1">
                <Button Content="Cancelar" Width="80" Margin="0,0,8,0" Click="Cancel_Click"/>
                <Button Content="Aplicar" Width="90" IsDefault="True" Click="Apply_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
